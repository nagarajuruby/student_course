<div class="card mb-4">
  <div class="card-body">
    <h4 class="text-center">New Course</h4>
  </div>
</div>

<%= render 'form', course: @course %>

<%= link_to 'Back', courses_path, class: "btn btn-outline-primary" %>
