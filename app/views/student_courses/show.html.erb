<p id="notice"><%= notice %></p>

<p>
  <strong>Student Name:</strong>
  <%= @student_course.student.name %>
</p>

<p>
  <strong>Course name:</strong>
  <%= @student_course.course.name %>
</p>

<%= link_to 'Edit', edit_student_course_path(@student_course), class: "btn btn-outline-primary" %> |
<%= link_to 'Back', student_courses_path, class: "btn btn-outline-primary"%>
